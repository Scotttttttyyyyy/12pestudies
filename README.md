# PE Studies Movement Master - Streamlit

## Run locally
```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

## Deploy
Upload these files to a GitHub repo, then deploy on Streamlit Community Cloud with main file path:
`streamlit_app.py`
